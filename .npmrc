# Improve optional dependency resolution
optional=true
# Use npm registry for consistent package resolution
registry=https://registry.npmjs.org/
# Enable optional peer dependencies
optional-peer-deps=true
# Don't save exact versions to allow for better semver resolution
save-exact=false